<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet (Week 6+ Retrofit) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notifications (Android 13+, Foreground Service 필수) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Activity Recognition (Step Counter, Android 10+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Location (Week 4 GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground Service (Android 14: Type 명시 필수) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Wake locks (산책 추적 중 화면 꺼짐 방지 옵션) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Health Connect 권한 (Week 5) — xml/health_permissions.xml에서 상세 정의 -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />

    <!-- Hardware features (지원 기기 필터링) -->
    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <queries>
        <!-- Health Connect 앱 존재 여부 체크용 -->
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>

    <application
        android:name=".WalkMateApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_walkmate_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_walkmate_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.WalkMate"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WalkMate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 디자인 시스템 검증용 (debug 빌드 전용 진입점, 사용자 비공개) -->
        <activity
            android:name=".ui.design.ColorTokenPreviewActivity"
            android:exported="false"
            android:label="Color Tokens"
            android:theme="@style/Theme.WalkMate" />

        <!-- Foreground Services (Week 2/4 활성화) — 현재 주석. 클래스 작성 후 enable.
        <service
            android:name=".service.StepCounterService"
            android:exported="false"
            android:foregroundServiceType="health" />
        <service
            android:name=".service.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
        -->

        <!-- Health Connect 권한 핸들러 (Week 5 활성화)
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
        -->

    </application>
</manifest>
